version: 0.1

phases:
  pre_build:
    commands:
      - echo Hello world
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - erik-resume.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
